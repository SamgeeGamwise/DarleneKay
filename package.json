{
    "name": "darlene-kay",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm:watch:sass\" \"npx @11ty/eleventy --serve --port=3000\"",
        "build:css": "sass --load-path=src/assets/styles src/assets/styles/main.scss:src/assets/css/main.css --style=compressed --no-source-map",
        "build": "npm run build:css && eleventy",
        "watch:sass": "sass --load-path=src/assets/styles src/assets/styles/main.scss:src/assets/css/main.css --style=expanded --embed-source-map --source-map-urls=relative --watch",
        "build:sass": "sass --load-path=src/assets/styles src/assets/styles/main.scss:src/assets/css/main.css --style=compressed --no-source-map"
    },
    "devDependencies": {
        "@11ty/eleventy": "^3.0.0",
        "concurrently": "^9.2.1",
        "postcss-cli": "^11.0.1",
        "purgecss": "^7.0.2",
        "sass": "^1.92.1"
    },
    "dependencies": {
        "@11ty/eleventy-img": "^6.0.4"
    }
}
